{
  "name": "aiter",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "dev"
  ],
  "scripts": {
    "setup-dev": "bun run scripts/setup-dev.ts",
    "dev": "cd dev && bun run src/index.tsx",
    "typecheck": "bun run typecheck:core && bun run typecheck:create-aiter-app",
    "typecheck:core": "cd packages/core && bun run typecheck",
    "typecheck:create-aiter-app": "cd packages/create-aiter-app && bun run typecheck",
    "build": "bun run build:core && bun run build:create-aiter-app",
    "build:core": "cd packages/core && bun run build",
    "build:create-aiter-app": "cd packages/create-aiter-app && bun run build",
    "create-aiter-app": "bun run packages/create-aiter-app/src/index.ts",
    "clean": "rm -rf packages/*/dist packages/*/node_modules node_modules"
  },
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.10",
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  }
}
